{
  "name": "one-trust-notices",
  "main": "./dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "node dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@interop-be-reports/commons": "1.0.0"
  }
}
